#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int x, y;
	int a;
	scanf("%d %d", &x, &y);
	if (x > 0) {
		if (y > 0) {
			a = 1;
		}
		else a = 4;
	}
	else if (x < 0) {
		if (y < 0) {
			a = 3;
		}
		else a = 2;
	}
	printf("%d\n", a);
}
